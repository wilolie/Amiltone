{% extends 'base.html.twig' %}

{% block title %}{{ student.name }} {{ student.firstname }}{% endblock %}

{% block body %}

<h1>{{ student.name }} {{ student.firstname }}</h1>
<div class="container mt-5 mb-4 text-center">
    <div class="row">
        <div class="col-3"></div>
        <div class="col-6 ">
            date de naissance : {{ student.birthday |date("d/m/Y") }} 
            <table class="table table-hover mt-5">
                <thead>
                    <tr>
                        <th scope="col">Matière</th> 
                        <th scope="col">note</th> 
                    </tr>
                </thead>
                <tbody>
                    {% for Note in Notes %}
                        <tr>
                            <th scope="row"> {{ Note.matiere }}</a>  </th>
                            <td>{{ Note.note | number_format(0,',',' ')}}/20</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
        <div class="col-3"></div>
    </div>
    <a href="{{path('add_note',{'id':student.id})}}" type="button" class="btn btn-primary">Ajouter une note</a>
    <a href="{{path('liste_Students')}}" type="button" class="btn btn-primary">retour</a>
</div>
{% endblock %}
